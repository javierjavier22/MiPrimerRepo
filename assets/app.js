/* Interacciones del mockup O.FRE.SER. Sin dependencias externas. */
const menuBtn=document.querySelector('.menu-btn');const mobileNav=document.querySelector('.mobile-nav');if(menuBtn&&mobileNav){menuBtn.addEventListener('click',()=>mobileNav.classList.toggle('open'));mobileNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileNav.classList.remove('open')))}
// Formulario comercial: arma una consulta de WhatsApp sin enviar datos a servidores del mockup.
const form=document.querySelector('#contactForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const lines=['Hola O.FRE.SER, quiero realizar una consulta.',`Nombre: ${data.get('nombre')||''}`,`Empresa/establecimiento: ${data.get('empresa')||''}`,`Tipo de consulta: ${data.get('tipo')||''}`,`Localidad: ${data.get('localidad')||''}`,`Teléfono: ${data.get('telefono')||''}`,`Mensaje: ${data.get('mensaje')||''}`];window.open('https://wa.me/5493875286093?text='+encodeURIComponent(lines.join('\n')),'_blank','noopener')})}
// Minijuego educativo: tres estaciones, una contiene la rata.
const game=document.querySelector('#ratGame');if(game){let hidden=Math.floor(Math.random()*3);const result=game.querySelector('.game-result');const stations=[...game.querySelectorAll('.station')];function reset(){hidden=Math.floor(Math.random()*3);stations.forEach(s=>s.classList.remove('reveal'));result.textContent='Elegí una estación. La idea es observar, registrar y decidir con evidencia.'}stations.forEach((s,i)=>s.addEventListener('click',()=>{stations[hidden].classList.add('reveal');result.textContent=i===hidden?'¡Correcto! Detectar actividad y ubicar dispositivos según evidencias es parte del monitoreo.':'No estaba ahí. En un programa MIP, la ubicación se define por señales de actividad, recorridos y condiciones del entorno.'}));const again=game.querySelector('[data-again]');if(again)again.addEventListener('click',reset);reset()}
